<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shruikan.bookmarks.com.bookmarks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- android:theme="@android:style/Theme.NoTitleBar.Fullscreen" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="preferences.PreferencesActivity"
            android:label="@string/action_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.BookmarkActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.AddBookmarkActivity" />
        <activity
            android:name=".activities.ListBookmarksActivity"
            android:label="@string/title_activity_list_bookmarks"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.EditBookmarkActivity" />
        <activity
            android:name="dialogs.DeleteDialog"
            android:label="@string/title_activity_delete_dialog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.AboutActivity"></activity>
    </application>

</manifest>